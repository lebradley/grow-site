const product = require('../../src/components/product');

describe('some tests', () => {
    it('should test a thing', () => {
        expect(1==1).to.equal(true);
        // put some test here
    });
});